"""
Top-level package for the Tomato timer application.

This package exposes:
- Core domain classes and utilities in ``tomato_app.core``
- Command line entry point logic in ``tomato_app.cli``

The idea is that other Python code can import and reuse the
core logic, while the CLI module focuses purely on argument
parsing and user interaction.
"""

